node_modules
dist
*.local
.DS_Store

# Environment variables — never commit secrets or local env files.
.env
.env.*

# Never ship installer binaries inside the Vite public tree (inflates Tauri bundles).
public/*.dmg
public/*.exe
public/*.app
