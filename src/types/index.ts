/**
 * Barrel de tipos del frontend.
 * El esquema canónico de flashcards locales vive en `schema.ts`.
 */

export type {
  Deck,
  Flashcard,
  RichCardContent,
} from "./schema";

export {
  DEFAULT_EASE_FACTOR,
  INITIAL_SRS_STATE,
} from "./schema";
